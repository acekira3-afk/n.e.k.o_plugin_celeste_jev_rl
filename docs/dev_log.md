# 开发日志

## 2026-09-23 — 数据测试（50集 + 100集）

### 今日完成

编写了端到端模拟测试脚本 `tests/test_first_data.py`，用 mock 环境验证 Think-Execute 三段式循环的完整架构，生成两批数据报告。

### 测试环境

- MockCelesteEnv：模拟蔚蓝环境，起点 x=0，目标 x=500，3% 随机死亡率
- MockJevAgent：基于简单启发式的 Jev 替身，70-350ms 模拟延迟
- 10 个内置帧精确脚本（dash_across_gap / wall_jump_climb / wave_dash_chain 等）
- PersonalityLayer：9 种表情 + 独白 + 位置记忆

### 50 集测试结果

| 指标 | 值 |
|------|-----|
| 脚本执行数 | 154 |
| 成功数 | 62 (34.2%) |
| 中断数 | 42 (27.3%) |
| 总思维数 | 159 |
| 位置记忆 | 27 |
| 50集耗时 | 69.9s |

### 100 集扩展测试结果

| 指标 | 值 |
|------|-----|
| 脚本执行数 | 307 |
| 成功数 | 124 (34.0%) |
| 死亡数 | 183 |
| 中断数 | 83 (27.0%) |
| 总思维数 | 113 |
| 位置记忆 | 41 |
| 总帧数 | 3,676 |
| Jev 调用数 | 307 |
| 100集耗时 | 139.2s |

### 各脚本成功率

| 脚本 | 使用次数 | 成功率 |
|------|---------|--------|
| simple_move_right | 32 | 100% |
| hyper_jump | 35 | 100% |
| dash_across_gap | 104 | 55% |
| wave_dash_chain | 36 | 0% |

### 关键发现

1. **架构验证通过**：Think-Execute 三段循环在 100 集中稳定运行，无崩溃
2. **中断检测有效**：27% 脚本被状态偏移打断，避免继续执行错误操作
3. **位置记忆系统工作**：41 个位置记住了历史失败/成功记录
4. **人格表情循环正确**：thinking → focused/panicked → happy/frustrated
5. **wave_dash_chain 0% 成功**：extreme 难度脚本在 mock 环境中风险太高，需要调整帧数据
6. **dash_across_gap 55% 成功**：中等成功率，主要死因是冲刺消耗后无法恢复
7. **0% 完成率**：mock 环境 3% 随机死亡率阻止通关，真实环境不会有此问题
8. **34% 整体成功率在 50/100 集中一致**：数据稳定性良好

### 真实游戏对接尝试

尝试通过 pyautogui + screencapture 对接 Steam 版蔚蓝：
- 游戏进程检测成功（PID 20228，CrossOver/Wine 运行）
- 截图捕获成功，能识别标题画面和主菜单
- 键盘输入存在映射问题：Enter/Space/C 键无法确认菜单
- 游戏可能需要控制器输入而非键盘，或需要通过 CrossOut 的 Wine 配置调整

### 下一步

1. 解决键盘输入映射问题（尝试控制器模拟或 key remapping）
2. 编写屏幕区域识别（OCR/像素匹配）提取真实游戏状态
3. 调整 wave_dash_chain 脚本帧数据
4. 用真实游戏截图训练视觉状态编码器

---

## 2026-09-22 — 三段式 Think-Execute 架构完成

### 今日完成

将蔚蓝 Jev RL 插件从"帧级 DQN 控制"重构为**三段式 Think-Execute 架构**：感知 → 思考 → 执行。核心思路来自用户的观察：蔚蓝的像素级操作可以通过编写帧精确操作脚本实现，Jev 负责判断当前状况选择对应脚本，Jev 的延迟反而成为"角色思考时间"。

### 关键设计决策

#### 1. 从"逐帧决策"到"情境-脚本映射"

之前的问题是：Jev 延迟 70-500ms，蔚蓝需要 16ms/帧的精度，根本来不及。

新方案：Jev 不选单帧动作，而是**识别当前情境**（冲刺间隙？蹬墙上升？尖刺走廊？），然后选择对应的**帧精确操作脚本**执行。这完全符合速通者的实际操作模式：看一眼障碍布局 → 想一下该用什么技巧 → 一套搓完。

#### 2. Jev 延迟从缺陷变成特性

之前：Jev 500ms 太慢
现在：Jev 500ms = "角色思考了 0.5 秒"，加上 0.3-1.0s 的拟人思考暂停，完全合理。N.E.K.O 人格层在这段时间显示 thinking 表情和独白文字。

#### 3. N.E.K.O 人格层让 AI 变成"角色"

- 9 种 Live2D 表情（thinking/focused/panicked/happy/frustrated 等）
- 独白系统（"前方有尖刺...得先跳上去"）
- 位置记忆（"上次在这选了 dash_across_gap 失败了，试试 wall_jump_climb"）

### 新增文件

| 文件 | 代码行数 | 说明 |
|------|---------|------|
| `core/script_library.py` | ~230 | 10 个内置帧精确脚本 + 脚本库管理 |
| `core/script_executor.py` | ~200 | 帧精确回放 + 5 种中断条件检测 |
| `core/personality.py` | ~270 | 9 表情 + 独白模板 + 位置记忆系统 |
| `core/jev_agent.py` | ~330 | 重写为情境识别模式（Choice + Noul + Score） |
| `core/experiment_runner.py` | ~250 | 重写为 ThinkExecuteRunner 三段循环 |

### 测试

- 36 个单元测试全部通过（1.54 秒）
- 覆盖：状态编码、脚本库、脚本执行器（含中断）、人格层（含记忆）、DQN、奖励塑形器

### 下一步

1. PICO-8 状态读取：编写自定义 cartridge 导出游戏内存
2. Browser Use 集成：通过 N.E.K.O 浏览器通道操作 Web 版 Celeste Classic
3. 脚本录制工具：录制速通操作并自动生成 FrameScript
4. DQN 脚本生成：用 DQN 学习新脚本并加入脚本库

---

## 2026-09-22（早） — 初始版本

### 完成内容

创建了 N.E.K.O 插件骨架，包含：

- 插件清单 `plugin.toml`（配置项、入口、依赖）
- Jev 决策层（4 层目标层级：战略→战术→动作→原子）
- DQN 训练层（4 层网络 + 经验回放 + 目标网络）
- Jev 奖励塑形器（危险惩罚 + 质量奖励）
- 实验调度器（4 模式对比：jev_only / dqn_only / jev_shaped_dqn / jev_router_dqn）
- 状态编码器（26 维数值 → 结构化文本）
- 12 个单元测试
- CI/CD workflow
- 详细实验方案文档

### 核心问题

帧级 Jev 控制不可行：70-500ms 延迟 > 16ms/帧需求。需要分层设计让 Jev 做高层决策，DQN 做底层执行。但这仍然需要训练 DQN。

### 转折

用户提出：蔚蓝的像素级操作可以通过**编写操作脚本**实现，现在状况判断进行延迟输入一系列操作，像是人物反应思考一段时间后的操作。这个思路直接绕过了"Jev 延迟太高"的问题，将延迟转化为特性。
